{{! It's done when only a logged in user can visit the /profile route.

It's done when a logged in user is redirected to /profile when they try to visit /login again. }}

<h1>Welcome {{user.name}}!</h1>

<h2>Create a New Project:</h2>

<div class='row'>
  <form id='project-form' class='col s7'>
    <div class='row'>
      <div class='input-field col s6'>
        <i class='material-icons prefix'>account_circle</i>
        <input
          id='pName'
          type='text'
          class='validate'
          required
          oninvalid="this.setCustomValidity('Please enter a title for your project')"
          oninput="this.setCustomValidity('')"
        />
        <label for='pName'>Project Title</label>
      </div>
      <div class='input-field col s6'>
        <i class='material-icons prefix'>password</i>
        <input
          id='pFund'
          type='number'
          step='0.01'
          min='0'
          class='validate'
          required
          oninvalid="this.setCustomValidity('Please enter the funding needed for you project')"
          oninput="this.setCustomValidity('')"
        />
        <label for='pFund'>Funding Needing ($)</label>
      </div>
    </div>
    <div class='row'>
      <div class='input-field col s12'>
        <textarea id='description' class='materialize-textarea'></textarea>
        <label for='description'>A description of your project...</label>
      </div>
    </div>
    <div class='row'>
      <button class='waves-effect waves-light btn' type='submit'><i
          class='material-icons right'
        >login</i>Submit</button>
    </div>
  </form>
  <div class="col s5">
    {{#each user.projects as |project|}}
    {{> userProjects-details}}
    {{/each}}
  </div>
</div>
<div>{{loggedIn}}</div>
<div>{{userId}}</div>

<script src="/js/newProject.js"></script>